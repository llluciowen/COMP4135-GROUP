# Render deployment requirements (Linux, Python 3.10, CPU only)
# Install with: pip install -r requirements_render.txt --extra-index-url https://download.pytorch.org/whl/cpu

# --- Web framework ---
Flask==3.0.0
gunicorn==21.2.0
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# --- Build tooling required at runtime by recbole (pkg_resources) ---
setuptools==69.0.3
wheel==0.42.0

# --- Scraping (used by scrape.py) ---
requests==2.31.0
lxml==4.9.3
pyquery==2.0.0
cssselect==1.2.0
charset-normalizer==3.3.2
certifi==2023.11.17
idna==3.6
urllib3==2.1.0

# --- Data / ML core ---
numpy==1.26.2
pandas==2.1.3
scipy==1.11.4
scikit-learn==1.4.0
joblib==1.3.2
threadpoolctl==3.2.0
tqdm==4.66.2

# --- NLP (used by content-based recs) ---
nltk==3.8.1
regex==2023.12.25

# --- Misc utils ---
python-dateutil==2.8.2
pytz==2023.3.post1
tzdata==2023.3
six==1.16.0

# --- SASRec (Algo Variant B) ---
# CPU-only torch wheel; --extra-index-url is set in Render Build Command
torch==2.4.1
recbole==1.2.1
pyyaml==6.0.1
tensorboard==2.16.2

# --- Classic CF (Algo Variant A) ---
# scikit-surprise has no manylinux wheel on PyPI and source build fails on
# Render's isolated build env. We disable it here; main.py automatically falls
# back to genre-affinity + Bayesian-smoothed popularity (see
# _recommend_without_surprise) which is shown to the user with a clear message.
# scikit-surprise==1.1.3
